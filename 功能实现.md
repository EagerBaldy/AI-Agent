# AI-Agent 项目面试指南

这份文档旨在帮助你全面理解本 AI-Agent 项目的架构与核心实现，以便在面试中从容应对关于 "功能具体是如何实现的？" 的提问。

## 1. 项目概览

本项目是一个基于 **Spring Boot** (后端) 和 **Vue 3** (前端) 的 AI 智能助手平台。它不仅仅是一个简单的聊天机器人，而是集成了 **ReAct Agent (推理与行动)** 模式、**RAG (检索增强生成)** 技术以及 **工具调用 (Tool Calling)** 能力的综合性 AI 应用。

### 核心技术栈
- **后端**: Java, Spring Boot, LangChain4j (核心 AI 框架)
- **前端**: Vue 3, Pinia, Vite
- **AI 模型**: Qwen (通义千问)

---

## 2. 核心模块与实现原理

### 2.1 Agent 核心引擎 (ReAct 模式)

**面试官提问**: "你们的 Agent 是怎么实现自主思考和调用工具的？"

**代码位置**: 
- `src/main/java/com/star/aicodehelper/agent/core/ReActAgent.java` (基类核心逻辑)
- `src/main/java/com/star/aicodehelper/agent/core/KoneManus.java` (具体实现)

**实现原理**:
本项目手动实现了一个轻量级的 **ReAct (Reasoning + Acting)** 循环。

1.  **思考循环 (`ReActAgent.execute`)**: 
    - Agent 接收用户输入，初始化上下文 (`AgentContext`)。
    - 进入 `while` 循环 (最大步骤限制 `MAX_STEPS = 10`)。
    - **Prompt 构建**: 将用户问题 + 历史记录 + 当前已执行的步骤 (Thought/Action/Observation) 拼接成 Prompt 发送给 LLM。
    - **解析响应**: 使用正则表达式 (`ACTION_PATTERN`) 解析 LLM 的输出。
        - 如果包含 `Action: xxx` 和 `Action Input: xxx`，则认为需要调用工具。
        - 如果包含 `Final Answer: xxx`，则认为任务结束。
2.  **工具执行**:
    - 如果解析出 Action，Agent 会根据工具名称调用相应的 Java 方法 (如 `webSearchTool.searchWeb`)。
    - 获取工具的返回结果 (Observation)。
3.  **结果回填**:
    - 将 `Observation` 添加到上下文中，作为下一次 Prompt 的一部分，再次询问 LLM。
    - LLM 根据观察结果继续思考，直到得出 `Final Answer`。

### 2.2 RAG (检索增强生成) 系统

**面试官提问**: "你们如何让 AI 回答私有领域知识（如代码库文档、特定业务知识）？"

**代码位置**:
- `src/main/java/com/star/aicodehelper/ai/rag/RagConfig.java` (知识库加载与配置)
- `src/main/java/com/star/aicodehelper/ai/AiCodeHelperServiceFactory.java` (服务组装)

**实现原理**:
使用了 LangChain4j 的 RAG 能力，流程如下：

1.  **文档加载 (ETL)**:
    - 启动时 (`RagConfig.initKnowledgeBaseRunner`)，扫描 `src/main/resources/docs/knowledge/` 下的 Markdown 文件。
    - 使用 `FileSystemDocumentLoader` 加载文档。
2.  **切分 (Splitting)**:
    - 使用 `DocumentSplitters.recursive(1000, 200)` 将长文档切分为较小的 `TextSegment`，保留一定的重叠以保持上下文。
3.  **嵌入与存储 (Embedding & Store)**:
    - 使用 Embedding Model (如 Qwen) 将文本段转换为向量。
    - 存储在 `InMemoryEmbeddingStore` (内存向量数据库) 中。本项目为不同领域 (code, travel, medical 等) 创建了独立的 Store 以隔离知识。
4.  **检索 (Retrieval)**:
    - 在 `AiCodeHelperServiceFactory` 中，通过 `AiServices.builder().contentRetriever(retriever)` 将检索器注入 AI 服务。
    - 当用户提问时，系统自动将问题转换为向量，在向量库中检索最相似的片段，拼接到 System Prompt 中传给 LLM。

### 2.3 高级 RAG 数据预处理 (ETL 优化)

**面试官提问**: "如果文档里有元数据或者复杂的结构，你们怎么处理来提高检索效果？"

**代码位置**:
- `src/main/java/com/star/aicodehelper/ai/rag/AdvancedMetadataTransformer.java`

**实现原理**:
我们不只是简单地读取文本，而是自定义了 `DocumentTransformer` 来清洗和结构化数据：
1.  **元数据提取**: 解析文档头部的 YAML Front Matter (如 `title`, `author`, `tags`)，将其存入 `Metadata` 对象。
2.  **结构识别**: 使用正则提取 Markdown 的标题 (H1, H2)，作为上下文补充。
3.  **特征标记**: 自动检测文档是否包含代码块 (`has_code`)，方便后续可能的过滤。
4.  **清洗**: 移除 Front Matter 等非正文内容，减少 Embedding 的噪声。

### 2.4 AI 安全护栏 (Guardrails)

**面试官提问**: "如何防止用户输入恶意内容或 Prompt 注入？"

**代码位置**:
- `src/main/java/com/star/aicodehelper/ai/guardrail/SafeInputGuardrail.java`

**实现原理**:
实现了 LangChain4j 的 `InputGuardrail` 接口：
1.  **拦截**: 在 LLM 处理之前，`validate` 方法会被自动调用。
2.  **检测**: 将用户输入转为小写，使用正则拆分单词。
3.  **过滤**: 检查是否包含敏感词集合 (如 `kill`, `evil`)。
4.  **拒绝**: 如果检测到敏感词，直接返回 `fatal` 结果，中断后续流程，不消耗 Token 也不让 LLM 接触恶意内容。

### 2.5 工具系统 (Tools)

**面试官提问**: "如何扩展 AI 的能力，比如让它能联网搜索或查天气？"

**代码位置**:
- `src/main/java/com/star/aicodehelper/ai/tools/WebSearchTool.java` (联网搜索实现)
- `src/main/java/com/star/aicodehelper/agent/core/KoneManus.java` (工具注册)

**实现原理**:
1.  **定义工具**: 创建 Java 类，使用 `@Tool` 注解标记方法，并提供详细的自然语言描述 (Description)。LLM 依靠这些描述来决定何时调用工具。
    - 例如 `WebSearchTool` 使用 Jsoup 爬取 Bing 搜索结果。
2.  **注册工具**:
    - 在 `KoneManus` (自定义 Agent) 中，通过 `addTool` 方法将工具名称、描述和 Lambda 表达式回调注册到 Agent 的工具列表中。
    - 或者在 `AiServices` 中直接通过 `.tools(...)` 注入。

### 2.6 后端服务层与 API

**代码位置**:
- `src/main/java/com/star/aicodehelper/controller/AgentController.java` (API 入口)
- `src/main/java/com/star/aicodehelper/ai/AiCodeHelperService.java` (LangChain4j 接口定义)

**实现原理**:
- **流式响应**: 接口 `/agent/chat` 返回 `Flux<String>`，利用 Spring WebFlux 实现 Server-Sent Events (SSE)，实现打字机效果。
- **会话管理**: 虽然目前是简单的内存/数据库记录，但在 `AgentController` 中处理了 `userId` 和 `sessionId`，用于区分不同用户的对话上下文。

---

## 3. 前端架构 (Vue 3)

**面试官提问**: "前端是如何与流式 AI 接口交互的？"

**代码位置**:
- `ai-code-helper-frontend/src/views/WorkspaceView.vue` (主工作区)
- `ai-code-helper-frontend/src/stores/chat.js` (状态管理与 API 调用)

**实现原理**:
1.  **SSE 消费**: 前端使用 `fetch` 或 `EventSource` (在 `chatApi.js` 或 `chat.js` 中实现) 连接后端的流式接口。
2.  **增量渲染**: 接收到后端推送的数据块 (Chunk) 后，实时拼接到当前消息内容中，给用户即时反馈。
3.  **多模式支持**: `WorkspaceView` 根据路由参数 (`mode`) 动态加载不同的主题配置 (`themes.js`) 和 API 端点 (虽然目前主要是通过 session 区分，但 UI 上做了区分)，实现了 "编程助手"、"旅游规划师" 等不同角色的切换。

### 3.1 前端状态管理 (Pinia)

**面试官提问**: "前端如何管理复杂的聊天状态和历史记录？"

**代码位置**:
- `ai-code-helper-frontend/src/stores/chat.js`

**实现原理**:
使用 Pinia 的 `useChatStore` 集中管理：
1.  **会话管理**: `sessions` 数组存储当前模式下的所有会话，支持创建、切换、重命名和删除。
2.  **消息流**: `messages` 数组存储当前会话的消息列表。
3.  **状态标记**: 
    - `isStreaming`: 标记是否正在接收 SSE 流。
    - `isAiTyping`: 用于显示 "对方正在输入" 的动画。
4.  **动作封装**: `loadHistory` 负责从后端拉取历史记录并格式化；`switchSession` 负责清理当前状态并加载新会话上下文。

### 3.2 前端 Markdown 渲染优化

**面试官提问**: "AI 返回的代码块怎么高亮？流式输出导致 Markdown 格式错乱怎么解决？"

**代码位置**:
- `ai-code-helper-frontend/src/components/ChatMessage.vue`

**实现原理**:
1.  **渲染引擎**: 使用 `marked` 库将 Markdown 文本转换为 HTML，并开启 `gfm` (GitHub Flavored Markdown) 支持。
2.  **流式格式修复**: 
    - 在流式接收过程中，由于网络分包，可能会导致 Markdown 语法被截断（如 `**` 变成了 `*`）。
    - 我们在 `computed` 属性中添加了正则表达式预处理：
        - 修复列表项缺少空格 (`-Item` -> `- Item`)
        - 修复标题缺少空格 (`#Title` -> `# Title`)
        - 修复列表/标题前缺少换行，确保渲染引擎正确识别块级元素。
3.  **代码高亮**: 集成 `highlight.js` (通常配合 marked 使用) 实现代码块的语法高亮。

---

## 4. DevOps 与运维

### 4.1 AI 可观测性 (Observability)

**面试官提问**: "怎么监控 Token 消耗？怎么调试 Prompt？"

**代码位置**:
- `src/main/java/com/star/aicodehelper/ai/listener/ChatModelListenerConfig.java`

**实现原理**:
实现了 LangChain4j 的 `ChatModelListener` 接口，作为全局拦截器：
1.  **onRequest**: 记录发给 LLM 的完整 Prompt，方便调试 Prompt Engineering 效果。
2.  **onResponse**: 记录 LLM 的原始响应，包括生成的文本和 Token 使用统计 (Input/Output Tokens)。
3.  **onError**: 捕获并记录与 LLM 交互过程中的异常（如超时、配额不足）。

### 4.2 容器化部署 (Docker)

**面试官提问**: "项目是怎么部署的？"

**代码位置**:
- `Dockerfile`

**实现原理**:
采用 **多阶段构建 (Multi-stage Build)** 优化镜像体积：
1.  **构建阶段 (Build Stage)**: 使用 `maven:3.9.6-eclipse-temurin-21-alpine` 镜像，执行 `mvn clean package` 编译打包。
2.  **运行阶段 (Run Stage)**: 使用轻量级的 `openjdk:21-jdk-slim` 镜像，仅复制构建产物 (`app.jar`)。
3.  **结果**: 最终镜像不包含 Maven 和源码，体积小且更安全。

---

## 5. 系统配置

### 5.1 跨域资源共享 (CORS)

**面试官提问**: "前后端分离怎么解决跨域问题？"

**代码位置**:
- `src/main/java/com/star/aicodehelper/config/CorsConfig.java`

**实现原理**:
实现了 Spring 的 `WebMvcConfigurer` 接口：
1.  **全局配置**: 通过 `addCorsMappings` 覆盖所有路径 (`/**`)。
2.  **安全策略**: 
    - `allowCredentials(true)`: 允许前端携带 Cookie/认证信息。
    - `allowedOriginPatterns("*")`: 允许所有域名访问（生产环境应限制为特定域名）。
    - 允许常用的 HTTP 方法 (GET, POST, etc.)。

---

## 7. 项目难点与解决方案

### 7.1 难点：ReAct Agent 陷入死循环
**问题描述**: Agent 有时会在思考过程中反复执行相同的 Action，或者在没有明确结果时一直空转，消耗大量 Token 并导致请求超时。
**解决方案**: 
1.  **最大步数限制**: 在 `ReActAgent` 中设置 `MAX_STEPS = 10`，强制终止过长的思考链。
2.  **重复检测机制**: 
    - 记录已执行过的 `(Action, Action Input)` 对。
    - 如果检测到完全相同的操作，强制 Agent 停止并输出当前最佳猜测，或者提示 Agent "你已经尝试过这个了，请换个思路"。
3.  **Prompt 优化**: 在 System Prompt 中明确指示 "如果无法找到答案，请直接说明，不要重复尝试"。

### 7.2 难点：流式响应下的 Markdown 渲染闪烁与格式错乱
**问题描述**: 后端 SSE 推送是按 Token 也就是字符片段推送的。如果 Markdown 标记（如 `**bold**` 或 ````java`）被截断，前端渲染引擎会暂时无法识别，导致页面布局跳动或显示源码。
**解决方案**: 
1.  **前端预处理**: 在 `ChatMessage.vue` 中使用正则对未闭合的 Markdown 标签进行临时补全（虽然本项目目前主要做的是空格修复，但这是通用的扩展方向）。
2.  **CSS 优化**: 为消息容器设置 `min-height` 和 `word-break`，减少因内容变化导致的布局重排 (Reflow)。
3.  **正则修复**: 正如前文所述，针对 LLM 经常输出不规范 Markdown (如列表缺空格) 的情况，在前端进行正则自动修正。

### 7.3 难点：RAG 检索相关性低 (检索噪音)
**问题描述**: 简单的文本切分导致语义断裂，检索到的片段虽然包含关键词，但缺乏上下文，导致 LLM 回答驴唇不对马嘴。
**解决方案**: 
1.  **结构化切分**: 实现 `AdvancedMetadataTransformer`，不再无脑切分，而是基于 Markdown 的章节结构进行切分。
2.  **元数据增强**: 提取文档的 Title, Tags 等元数据，并在检索时给予元数据更高的权重（本项目通过将元数据拼接到 TextSegment 中模拟这一效果）。
3.  **重叠窗口**: 设置 `overlap = 200`，确保切分点附近的上下文被保留。

### 7.4 难点：Bean 循环依赖
**问题描述**: `AiCodeHelperServiceFactory` 依赖 `RagConfig` (获取 Retriever)，而 `RagConfig` 可能又依赖其他 Service，导致 Spring 启动报错。
**解决方案**: 
1.  **使用 `@Lazy` 注解**: 在注入 `ContentRetriever` 时添加 `@Lazy`，推迟 Bean 的初始化，打破循环依赖链。
2.  **重构配置**: 将配置类职责单一化，避免在一个 Config 类中既定义 Bean 又注入复杂的业务 Bean。

---

## 8. 总结：如何回答 "功能 XX 是怎么实现的？"

**模板**:
"这个功能主要位于 `[模块名]` 模块。具体来说，我们使用了 `[核心技术/库]`。流程是这样的：首先 `[步骤1]`，然后 `[步骤2]`... 比如在代码中，`[类名]` 负责了 `[核心逻辑]`..."

**示例：回答 "你们的 Agent 怎么知道什么时候该搜索？"**
"这主要是在 `ReActAgent` 和 `KoneManus` 类中实现的。我们在 `KoneManus` 中注册了 `WebSearchTool`，并给它写了一段描述 'Useful for when you need to answer questions about current events...'。
在 `ReActAgent` 的执行循环中，我们将用户问题和这个工具描述一起发给 LLM。LLM (Qwen) 如果判断需要外部信息，就会返回一个符合我们正则规则的字符串 `Action: Search`。我们的代码解析到这个 Action 后，自动调用 `WebSearchTool`，把搜索结果作为 Observation 再喂回给 LLM，它拿到信息后就能回答用户了。"