我将按照以下计划实现所要求的功能：

&#x20;

### 后端：网络化搜索

1. **创建** **`WebSearchTool`**：实现一个新的工具类 `com.yupi.aicodehelper.ai.tools.WebSearchTool`，该类将使用 `Jsoup`来执行网络搜索（在此环境中通过模拟抓取必应搜索页面实现）并返回结果。
2. **注册工具**：更新 `AiCodeHelperServiceFactory`，以将 `WebSearchTool`注册到AI服务（`AiCodeHelperService`、`AiTravelHelperService`等）中。

### 前端：UI/UX增强

1. **暂停生成**：
   * 在 `App.vue`中，添加一个“停止生成”按钮，该按钮在 `isStreaming`为 true 时出现。
   * 实现点击处理程序，以调用 `currentEventSource.close()`并更新状态。
2. **可折叠对话**：
   * 修改 `ChatMessage.vue`，添加一个 `isCollapsed`状态。
   * 在消息头部添加一个切换按钮（图标）来折叠/展开消息内容。
3. **主页按钮**：
   * 在 `App.vue`中，为主页上的每个助手卡片添加一个“开始辅导”按钮，与视觉风格保持一致。
4. **Markdown格式修复**：
   * 在 `ChatMessage.vue`中，更新Markdown渲染逻辑，以对文本进行预处理。
   * 使用正则表达式确保列表项（以 `-`、`*`或 `1.`开头的行）前面有一个双换行符，这样它们就能正确渲染为HTML列表。

### 验证

* 通过询问需要当前信息的问题来验证网络搜索功能。
* 验证暂停按钮是否能够停止输出。
* 验证折叠消息功能是否正常工作。
* 验证Markdown列表是否正确渲染。

