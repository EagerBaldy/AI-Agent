I will help you dockerize the project and prepare it for deployment to `www.kone.js.cn/proj/ai-agent`.

Here is the implementation plan:

1.  **Frontend Configuration (`ai-code-helper-frontend/vite.config.js`)**
    *   Update `base` to `'/proj/ai-agent/'` so the application loads resources correctly from the subpath.

2.  **Frontend Docker Setup**
    *   Create `ai-code-helper-frontend/Dockerfile`: Multi-stage build (Node.js for building, Nginx for serving).
    *   Create `ai-code-helper-frontend/nginx.conf`: Custom configuration to handle SPA routing (Single Page Application) and serve static files.

3.  **Backend Docker Setup**
    *   Create `Dockerfile` in the project root: Build the Spring Boot application using Maven and run it with JDK 17 (or appropriate version).

4.  **Database Setup**
    *   Include a MySQL service in `docker-compose.yml`.
    *   Configure it to automatically initialize using your existing `sql/create_table.sql`.

5.  **Orchestration (`docker-compose.yml`)**
    *   Define services: `frontend`, `backend`, `db`, and `nginx-gateway`.
    *   Configure networking and environment variables (specifically overriding the DB host for the backend).

6.  **Gateway Configuration (`nginx.conf`)**
    *   Create a main Nginx configuration to route traffic:
        *   `www.kone.js.cn/proj/ai-agent` -> Frontend Container
        *   `www.kone.js.cn/api` -> Backend Container

7.  **Deployment Script**
    *   Provide a shell script (`deploy.sh`) to build and run the project on the server.

### Summary of New Files
*   `ai-code-helper-frontend/Dockerfile`
*   `ai-code-helper-frontend/nginx.conf`
*   `Dockerfile` (Backend)
*   `docker-compose.yml`
*   `nginx/conf.d/default.conf` (or `nginx.conf`)
*   `deploy.sh`

### Verification
*   I will verify the file content and structure.
*   Since I cannot run Docker inside this environment, I will provide the exact commands you need to run on your server.
